# Frozen POM-RX v0.1 compatibility boundary

Protected canonical material currently remains at its historical paths, including:

- `sdk/typescript/pom-rx.mjs`;
- `fixtures/pom-rx/v0.1-compat/`;
- published canonical bytes, hashes and source pins that reference those paths.

A future physical move requires byte/hash parity, import/link compatibility and explicit migration evidence. This directory is an ownership marker, not a replacement path.
