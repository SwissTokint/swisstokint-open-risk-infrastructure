# POM-RX Core control plane

This directory is the versioned, canonical control plane for POM-RX Core in the
single repository `SwissTokint/swisstokint-open-risk-infrastructure`.

The former directory
`C:\Dev\SwissTokint\project-management\pom-rx-core` is a read-only migration
archive after this control-plane PR merges. Do not maintain two writable copies.

Git and GitHub exact-head evidence override stale register text. Each useful lot
must be committed and pushed to a dedicated branch before cycle end. Dirty or
unowned work is preserved before cleanup; no unpublished file is deleted.

The website, Vercel and funding dossier remain outside this repository task.
