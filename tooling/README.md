# Repository tooling

Build, CI, migration and developer automation ownership lives here conceptually. Existing canonical tooling remains under `.github/` and `scripts/` until separately migrated when doing so is useful and compatibility-safe.
