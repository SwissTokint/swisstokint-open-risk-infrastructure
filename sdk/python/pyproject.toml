[project]
name = "swisstokint-proof-of-method"
version = "0.2.0"
description = "SwissTokint Proof of Method Receipt v0.2 SDK"
requires-python = ">=3.10"
dependencies = ["cryptography>=42"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["swisstokint_proof"]
